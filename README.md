# FSA-Training
This app runs on FastAPI backend server.
To run application, create a virtual environment on your machine, install uvicorn, and run the following command:

uvicorn app: app --reload

Can visit locally hosted site at following URL:
http://127.0.0.1:8000